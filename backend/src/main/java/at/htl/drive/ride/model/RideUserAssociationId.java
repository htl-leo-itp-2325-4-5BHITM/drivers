package at.htl.drive.ride.model;

import jakarta.persistence.Embeddable;

import java.io.Serializable;
import java.util.Objects;
/*
@Embeddable
public class RideUserAssociationId implements Serializable {

    private Long RideId;
    private Long UserId;

    public RideUserAssociationId() {

    }

    public RideUserAssociationId(Long rideId, Long userId) {
        RideId = rideId;
        UserId = userId;
    }

    public Long getRideId() {
        return RideId;
    }

    public void setRideId(Long rideId) {
        RideId = rideId;
    }

    public Long getUserId() {
        return UserId;
    }

    public void setUserId(Long userId) {
        UserId = userId;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        RideUserAssociationId that = (RideUserAssociationId) o;
        return Objects.equals(RideId, that.RideId) && Objects.equals(UserId, that.UserId);
    }

    @Override
    public int hashCode() {
        return Objects.hash(RideId, UserId);
    }
}*/
